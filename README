Audio graph constraints
=======================

Modeling an audio graph with signal processors.

Optimize placement of resamplers in the graph.

## How to use the models

You need a constraint solver that can get minizinc files as inputs.
Tested with minizinc, gecode, and minizincIDE.
